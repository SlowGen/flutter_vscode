import * as vscode from 'vscode';

export function subscribeToGeneratedContent(context: vscode.ExtensionContext, provider: vscode.WebviewViewProvider) {

  // The generator will add the subscriptions here
}
