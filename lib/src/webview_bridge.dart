/// A bridge to communicate with the VS Code extension.
class WebViewBridge {
  /// Sends a message to the VS Code extension.
  void postMessage(dynamic message) {
    // TODO: Implement using package:web instead of deprecated dart:js_util
    // This is a simplified stub - full implementation would need proper js_interop setup
    // For production use, consider using package:web instead of direct web libraries
  }
}

