const String commandHandlerTemplate = r'''
///**
// This file is a temporary placeholder until the dart build_runner
// creates the generated file with all of the created commands
// */


import * as vscode from 'vscode';

export function handleCommand(message: { command: string, [key: string]: any }, panel: vscode.WebviewPanel) {
    const { command } = message;

    switch (command) {
        // This will be generated by build_runner
        default:
            vscode.window.showInformationMessage(`Unknown command: ${command}`);
    }
}

''';
