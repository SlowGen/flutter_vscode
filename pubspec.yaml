name: flutter_vscode
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # Add this for the annotations
  meta: ^1.9.1
  # Path package for file manipulation in tools
  path: ^1.8.3
  # Build dependencies needed for code generation
  build: ^3.0.0
  source_gen: ^3.1.0
  analyzer: ^7.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # Add these for the build_runner
  build_runner: ^2.4.6
  very_good_analysis: ^9.0.0

executables:
  generate_vscode_extension: generate_vscode_extension
